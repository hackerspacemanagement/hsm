.hero-unit
  .pull-right= image_tag 'logo.png'

  %h1 HeatSync Labs

  %h2 Tools
  %table
    %thead
      %td= "Name"
      %td= "Owner"
      %td= "Description"
    %tbody
      - @Tools.each do |tool|
        %tr
          %td= tool.name
